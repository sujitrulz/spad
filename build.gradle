plugins {
    id "org.sonarqube" version "3.3"
}

sonarqube {
    properties {
        property "sonar.projectKey", "sujitrulz_spad"
        property "sonar.organization", "spad"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}